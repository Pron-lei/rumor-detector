# 人工智能导论大作业 — 可解释的谣言检测 项目指导手册

> 团队规模：4人 | 满分：100分  
> 数据集：train.csv (2840条) / val.csv (401条) | 7个事件主题

---

## 一、项目目标与评分标准

### 核心目标

| 输出 | 要求 | 负责模块 |
|------|------|----------|
| **输出1：二分类** | 0=非谣言 / 1=谣言，准确率越高越好 | 分类模型（深度学习） |
| **输出2：判断依据** | 一段文字，解释分类依据 | 解释模型（LLM） |

### 评分标准（满分100）

| 评分项 | 分值 | 关键点 |
|--------|------|--------|
| 大作业报告叙述清楚 | 30分 | 结构清晰，逻辑严谨，≤2000字 |
| 代码可运行，部署说明清楚 | 25分 | README完善，可一键复现 |
| 检测分类准确率 | 15分 | val.csv上的accuracy |
| 判断依据可解释性 | 15分 | 解释合理、有说服力 |
| 小组分工协作 | 15分 | 代码仓库贡献记录 |

---

## 二、数据集说明

### 数据结构

```
id, text, label, event
```

| 字段 | 说明 |
|------|------|
| id | 推文唯一ID |
| text | 推文内容（英文） |
| label | 0=非谣言，1=谣言 |
| event | 事件主题类别（0~6，共7类） |

### 数据分布

| 数据集 | 总数 | 非谣言(0) | 谣言(1) | 事件数 |
|--------|------|-----------|---------|--------|
| train.csv | 2840 | 1600 | 1240 | 7 |
| val.csv | 401 | 226 | 175 | 7 |

> 标签分布相对均衡，存在轻微不平衡，可考虑使用加权损失或数据增强。

---

## 三、4人分工方案

### 👤 A — 组长：项目统筹 + 系统集成 + 部署文档 + 报告统稿

**核心职责：**

- 创建并管理 GitHub 公开仓库，设置分支策略（main / dev / 个人分支）
- 制定任务时间表，追踪各成员进度
- 搭建端到端推理 Pipeline：输入文本 → 预处理 → 分类 → 解释 → 输出
- 实现最终 `RumourDetectClass` 推理类接口
- 编写 README.md（环境配置、安装步骤、运行方法、项目结构说明）
- 统稿 report.pdf，确保风格统一，字数控制在 2000 字以内

**关键产出：**
- GitHub 仓库 + README.md
- `inference.py` 端到端推理脚本
- `RumourDetectClass` 最终调用类
- report.pdf（统稿）

---

### 👤 B — 分类模型负责人

**核心职责：**

- 数据探索分析（EDA）：文本长度分布、词频统计、事件分布、标签分布可视化
- 文本预处理：清洗（去URL/mention/hashtag）、分词、构建词表
- 实现基线模型：TF-IDF + 逻辑回归（参考支持文档代码）
- 实现深度学习模型：
  - BiGRU（参考支持文档代码，作为深度学习基线）
  - BERT / RoBERTa / BERTweet 微调（HuggingFace Transformers）
- 超参数调优，追求最优验证集准确率
- 输出训练好的模型 checkpoint

**关键产出：**
- `preprocess.py` 数据预处理脚本
- `train_baseline.py` 逻辑回归基线训练
- `train_bigru.py` BiGRU模型训练
- `train_bert.py` BERT微调训练
- `models/` 模型checkpoint目录

**技术建议：**

```
优先路线：
  1. TF-IDF + LR（快速基线，预期 ~75-80%）
  2. BiGRU（深度学习基线，预期 ~80-85%）
  3. BERT-base-uncased 微调（主力模型，预期 ~88-93%）
  4. 可选：RoBERTa、BERTweet（推文专用预训练模型）
```

---

### 👤 C — 解释模型负责人

**核心职责：**

- 对接上海交通大学大模型 API（https://claw.sjtu.edu.cn/guide/sjtu-api/）
- 设计 Prompt 模板，引导 LLM 输出结构化的判断依据
- 实现 RAG 增强模块（可选但推荐）：
  - 检索同事件主题下的相关推文作为上下文
  - 利用检索结果增强 LLM 的解释质量
- 评估解释质量（一致性、合理性、是否指向文本中的具体证据）
- 撰写报告中"判断依据模型"相关章节

**关键产出：**
- `llm_api.py` LLM调用封装
- `prompts.py` Prompt模板管理
- `explain.py` 解释生成模块
- （可选）`rag.py` RAG检索增强模块

**Prompt 设计建议：**

```
System: 你是一个谣言检测分析专家。给定一条推文和分类结果，
        请用中文解释为什么这条推文被判定为谣言/非谣言。
        要求：引用推文中的具体内容作为证据，解释要有逻辑性。

User:   推文内容：{text}
        检测结果：{label}（{label_text}）
        置信度：{confidence}

        请分析判断依据：
```

---

### 👤 D — 实验评估 + 可视化 + 报告撰写

**核心职责：**

- 设计对比实验方案：
  - 多模型横向对比（LR / BiGRU / BERT / BERT+LLM）
  - 消融实验（有/无 RAG、不同 Prompt 策略、不同预训练模型）
- 实验结果统计与可视化（准确率对比柱状图、混淆矩阵、F1/精确率/召回率表格）
- 解释质量评估（随机抽样 50 条，人工/半人工评估解释合理性）
- 撰写报告中的核心章节：选题背景、实验结果与分析
- 协助组长完成最终报告润色与排版

**关键产出：**
- `evaluate.py` 统一评估脚本
- `visualize.py` 可视化图表生成
- 实验对比结果表格
- 报告中的"实验结果"章节

---

## 四、项目实施阶段（共5阶段）

### 🔷 第一阶段：环境搭建与数据探索（1-2天）

| # | 任务 | 负责人 | 产出 |
|---|------|--------|------|
| 1.1 | 创建 GitHub 公开仓库，设置分支规则 | A | 仓库地址 + .gitignore |
| 1.2 | 配置 conda 环境，安装 PyTorch / Transformers 等依赖 | A + B | `requirements.txt` |
| 1.3 | 数据 EDA：分析文本长度、标签分布、事件分布 | B | EDA 图表 |
| 1.4 | 文本预处理：清洗、分词探索 | B | 预处理原型 |
| 1.5 | 调研并测试 SJTU LLM API 连通性 | C | API 调用 Demo |
| 1.6 | 文献调研：谣言检测 + 可解释 AI 相关论文 | D | 调研笔记 |

**阶段一检查点：** 全组环境统一，数据理解一致，API 可连通

---

### 🔷 第二阶段：基线模型开发（2-3天）

| # | 任务 | 负责人 | 产出 |
|---|------|--------|------|
| 2.1 | TF-IDF + 逻辑回归基线训练与评估 | B | 基线准确率基准（~75-80%） |
| 2.2 | BiGRU 模型实现与训练 | B | `bigru.pt` + 评估结果（~80-85%） |
| 2.3 | 建立统一评估框架（accuracy/precision/recall/F1） | B + D | `evaluate.py` |
| 2.4 | 初版 Prompt 设计（零样本解释） | C | `prompts.py` v1 |
| 2.5 | 打通基础推理 Pipeline（分类→解释） | A | `inference.py` 原型 |

**阶段二检查点：** 两个基线模型可运行，端到端流程初步打通

---

### 🔷 第三阶段：高级模型开发 ⭐ 核心阶段（3-4天）

| # | 任务 | 负责人 | 产出 |
|---|------|--------|------|
| 3.1 | BERT-base-uncased 微调训练 | B | `bert_model/` checkpoint |
| 3.2 | 尝试 RoBERTa / BERTweet 等其他预训练模型 | B | 多模型对比素材 |
| 3.3 | 数据增强（如果类别不平衡影响大） | B | 增强训练集 |
| 3.4 | Prompt 优化：Few-shot / Chain-of-Thought | C | `prompts.py` v2、v3 |
| 3.5 | RAG 检索增强模块开发（可选） | C | `rag.py` |
| 3.6 | 解释生成与分类模型对接 | A + C | 完整推理流程 |
| 3.7 | 端到端集成测试 | A | 集成测试通过 |

**阶段三检查点：** 主力模型（BERT）训练完成，解释模块可输出合理依据

---

### 🔷 第四阶段：实验评估与优化（2-3天）

| # | 任务 | 负责人 | 产出 |
|---|------|--------|------|
| 4.1 | 所有模型在 val.csv 上的全面对比 | D | 对比结果表格 + 图表 |
| 4.2 | 消融实验（模块贡献分析） | D | 消融实验结果 |
| 4.3 | 解释质量评估（抽样 ≥50 条，评分标准：相关性/具体性/逻辑性） | C + D | 解释质量报告 |
| 4.4 | 推理速度测试（端到端耗时） | A | 运行时间记录 |
| 4.5 | 根据评估结果，做最终模型调优 | A + B + C | 最终版本模型 |

**阶段四检查点：** 实验结果完整，确定最终提交版本

---

### 🔷 第五阶段：文档撰写与提交（2-3天）

| # | 任务 | 负责人 | 产出 |
|---|------|--------|------|
| 5.1 | report.pdf 选题背景章节 | A + D | 约500字 |
| 5.2 | report.pdf 模型设计章节（总体+分类+解释+环境） | A + C | 约600字 |
| 5.3 | report.pdf 实验结果章节（性能+解释分析） | D | 约600字 |
| 5.4 | report.pdf 小组分工章节 | A | 约300字 |
| 5.5 | README.md 完善（部署安装 + 运行说明 + 项目结构） | A | 最终 README |
| 5.6 | 代码清理、注释补齐、最终检查 | B + C | 整洁代码仓库 |
| 5.7 | 组长在 Canvas 提交 GitHub 地址 | A | 提交确认 |

**阶段五检查点：** 所有文件齐全，GitHub 仓库整洁可复现

---

## 五、技术架构图

```
                      输入推文
                         │
                         ▼
              ┌─────────────────────┐
              │     文本预处理       │
              │  · URL/mention清洗   │
              │  · 分词/tokenize     │
              └──────────┬──────────┘
                         │
                         ▼
              ┌─────────────────────┐
              │    分类模型 (BERT)    │  ← B负责
              │  · HuggingFace微调   │
              │  · 输出: label + conf │
              └──────────┬──────────┘
                         │
               text + label + conf
                         │
                         ▼
              ┌─────────────────────┐
              │   解释生成 (LLM)      │  ← C负责
              │  · SJTU API调用      │
              │  · Prompt模板        │
              │  · RAG增强(可选)     │
              └──────────┬──────────┘
                         │
                         ▼
              最终输出
        { label, confidence, explanation }
```

---

## 六、项目目录结构建议

```
rumor-detector/
├── README.md                 # 项目说明 + 部署安装 + 运行方法
├── report.pdf                # 大作业报告（≤2000字）
├── requirements.txt          # Python依赖
├── data/
│   ├── train.csv             # 训练集
│   └── val.csv               # 验证集
├── models/                   # 训练好的模型checkpoint
│   ├── lr_model.pkl          # 逻辑回归
│   └── bert_model/           # BERT微调模型
├── src/
│   ├── preprocess.py         # 数据预处理
│   ├── train_baseline.py     # 逻辑回归训练
│   ├── train_bigru.py        # BiGRU训练
│   ├── train_bert.py         # BERT微调训练
│   ├── evaluate.py           # 统一评估脚本
│   ├── llm_api.py            # LLM API封装
│   ├── prompts.py            # Prompt模板
│   ├── explain.py            # 解释生成模块
│   ├── rag.py                # RAG检索增强（可选）
│   └── inference.py          # 端到端推理 + RumourDetectClass
├── notebooks/
│   ├── eda.ipynb             # 数据探索分析
│   └── result_analysis.ipynb # 实验结果分析
├── outputs/                  # 输出结果
│   ├── predictions.csv       # 验证集预测结果
│   └── figures/              # 可视化图表
└── docs/                     # 补充文档
    └── API使用说明.md         # SJTU LLM API使用记录
```

---

## 七、时间线总览

```
         Week 1              Week 2              Week 3              Week 4
   ─────────────────────────────────────────────────────────────────────────
   │ 阶段一 │  阶段二  │     阶段三（核心）    │  阶段四  │ 阶段五  │
   │ 环境   │  基线   │    BERT+LLM开发      │  评估   │  文档   │
   │ 搭建   │  开发   │                      │  优化   │  提交   │
   ─────────────────────────────────────────────────────────────────────────
     1-2天    2-3天             3-4天              2-3天      2-3天
```

---

## 八、关键注意事项

1. **LLM API 限定**：解释模块若使用大语言模型，必须使用上海交大提供的模型接口（https://claw.sjtu.edu.cn/guide/sjtu-api/），方便助教复现评测。

2. **代码仓库贡献**：从项目开始就建立 GitHub 仓库，每位成员应积极向仓库提交代码和文档，这是"小组分工协作（15分）"的重要依据。

3. **报告字数限制**：report.pdf 不超过 2000 字，务必精简，突出重点。

4. **可复现性**：提交的代码必须能运行，README 要写清楚环境配置和运行步骤（占 25 分）。

5. **雷同作业**：经查实雷同会被扣分，参考公开资源时注意适度修改和消化吸收。

6. **推荐技术栈**：
   - 分类：`transformers` (HuggingFace) + `torch`
   - 解释：SJTU LLM API（需提前测试连通性和速率限制）
   - 评估：`scikit-learn` metrics
   - 可视化：`matplotlib` / `seaborn`

7. **最低运行要求**：模型需能接受单条文本输入，输出分类结果 + 判断依据。确保推理时间合理（单条 < 10 秒）。

---

> 📌 本手册为项目全流程指导，各阶段具体细节可在实施过程中灵活调整。  
> 📌 组长（A）负责整体进度把控，建议每 2-3 天进行一次简短同步会议。
